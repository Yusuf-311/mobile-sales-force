{
  "name": "msf-backend",
  "version": "1.0.0",
  "description": "Mobile Sales Force — Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "fast-check": "^3.20.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
